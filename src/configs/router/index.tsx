import React from 'react'
// import { Route } from 'react-router-dom'

// import routes from './router'

// function createRoute({ path, component }: any, index: number) {
//   return <Route path={path} exact={true} component={component} key={index} />
// }

// const router = () => routes.map((route, index) => createRoute(route, index))

// export default router
export default {}
